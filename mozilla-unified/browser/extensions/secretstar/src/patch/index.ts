// When running the development server, it will fail if browser?.berytus?.*
// is undefined, so we provide a patch to development PURE components only.
// The patch is not meant to be a correct representation but enough
// to render PURE components
import "./webext";